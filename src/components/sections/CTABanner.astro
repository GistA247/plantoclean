---
const trustBadges = [
  { icon: "verified", label: "Zertifizierter Betrieb" },
  { icon: "map", label: "Einsatzgebiet ganz NRW" },
  { icon: "recycling", label: "Nachhaltige Entsorgung" },
];
---

<section class="bg-primary py-20 relative overflow-hidden">
  <!-- Decorative Background Icon -->
  <span class="material-icons text-[400px] text-white opacity-10 absolute top-0 right-0 leading-none select-none pointer-events-none">spa</span>

  <div class="relative z-10 max-w-4xl mx-auto px-6 text-center">
    <!-- Title -->
    <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
      Bereit für einen Neuanfang in Ihrem Garten?
    </h2>

    <!-- Subtitle -->
    <p class="text-white/80 text-lg max-w-2xl mx-auto mb-10 leading-relaxed">
      Kontaktieren Sie uns für ein kostenloses Erstgespräch und ein unverbindliches Angebot.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12">
      <a
        href="/kontakt"
        class="inline-flex items-center gap-2 bg-white text-primary font-semibold px-8 py-4 rounded-full hover:bg-white/90 transition-colors"
      >
        Jetzt Angebot anfordern
        <span class="material-icons text-xl">arrow_forward</span>
      </a>

      <a
        href="/kontakt"
        class="inline-flex items-center gap-2 border-2 border-white/30 text-white font-semibold px-8 py-4 rounded-full hover:bg-white/10 transition-colors"
      >
        <span class="material-icons text-xl">phone</span>
        Rückruf vereinbaren
      </a>
    </div>

    <!-- Trust Badges -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-6 sm:gap-10">
      {trustBadges.map((badge) => (
        <div class="flex items-center gap-2 text-white/70">
          <span class="material-icons text-lg">{badge.icon}</span>
          <span class="text-sm font-medium">{badge.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>
